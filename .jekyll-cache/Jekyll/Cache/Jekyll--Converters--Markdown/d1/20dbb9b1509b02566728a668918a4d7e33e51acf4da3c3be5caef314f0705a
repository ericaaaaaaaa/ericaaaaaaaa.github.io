I")<p>在 parse 函数遇到 <code class="language-plaintext highlighter-rouge">auto *rparent = new Rparent(*(iter++));</code> 时将其替换为先检查当前 iter 的 Token 类是否为 RPARENT，若是，则重复之前的操作，若不是，则自动补全右小括号并抛出 j 型错误</p>
:ET